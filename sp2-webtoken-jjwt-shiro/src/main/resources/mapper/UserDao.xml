<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zgy.learn.webtoken.mapper.UserDao">

    <resultMap type="com.zgy.learn.webtoken.pojo.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="perms" column="perms" jdbcType="VARCHAR"/>
        <result property="role" column="role" jdbcType="VARCHAR"/>
        <result property="salt" column="salt" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select
          id, name, password, perms, role, salt
        from security.user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select
          id, name, password, perms, role, salt
        from security.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, name, password, perms, role, salt
        from security.user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="perms != null and perms != ''">
                and perms = #{perms}
            </if>
            <if test="role != null and role != ''">
                and role = #{role}
            </if>
            <if test="salt != null and salt != ''">
                and salt = #{salt}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into security.user(name, password, perms, role, salt)
        values (#{name}, #{password}, #{perms}, #{role}, #{salt})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into security.user(name, password, perms, role, salt)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.password}, #{entity.perms}, #{entity.role}, #{entity.salt})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into security.user(name, password, perms, role, salt)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.password}, #{entity.perms}, #{entity.role}, #{entity.salt})
        </foreach>
        on duplicate key update
        name = values(name) , password = values(password) , perms = values(perms) , role = values(role) , salt =
        values(salt)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update security.user
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="perms != null and perms != ''">
                perms = #{perms},
            </if>
            <if test="role != null and role != ''">
                role = #{role},
            </if>
            <if test="salt != null and salt != ''">
                salt = #{salt},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from security.user where id = #{id}
    </delete>

</mapper>